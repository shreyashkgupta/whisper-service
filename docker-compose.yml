---
# Auto-generated by Cloud Code AI
version: '3.7'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes: [.:/app]
    ports: [5000:5000]
  nginx:
    image: nginx:latest
    volumes: [./nginx.conf:/etc/nginx/nginx.conf]
    ports: [80:80]
    depends_on: [app]
